// ZombieCow is the same entity as Cow used in wave-based arena mode.
// Kept as a separate file to allow future divergence (different stats, FX, etc.)
export { createCow as createZombieCow } from './cow.js'
